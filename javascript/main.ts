import { reposIndex, repoPath } from './repos';

import './basic-component';
import './nested-mike';
import './tours-view';
import './tour-view';

document.addEventListener("DOMContentLoaded", () => {
  reposIndex();
  repoPath();
});

